test:
1. /dev
2. file names have to be all together after flags
or error message
test: ./ft_ls -a
./ft_ls /dev srcs -l
./ft_ls -a /dev -l libft
./ft_ls -l /tmp for "l-----s---"
./ft_ls -l /bin/ps -----s---"
if flag is repeting, do it once


./ft_ls -l srcs Makefile #print file first then dir#


handle / root
chomd sticky bits
chomd 000 for no authoriation

lstat to tele whether it is a dir
